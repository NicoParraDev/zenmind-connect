# 🚀 Instrucciones para Git Bash

## ✅ Opción 1: Ejecutar desde CMD (RECOMENDADO)

Abre **CMD** (no Git Bash) y ejecuta:
```bash
INICIAR_VIDEOLLAMADA.bat
```

O haz doble clic en `INICIAR_VIDEOLLAMADA.bat` desde el explorador de archivos.

---

## ✅ Opción 2: Ejecutar desde Git Bash

Desde Git Bash, ejecuta:
```bash
./iniciar_videollamada.sh
```

O:
```bash
bash iniciar_videollamada.sh
```

---

## ✅ Opción 3: Usar cmd.exe desde Git Bash

Desde Git Bash, ejecuta:
```bash
cmd.exe /c INICIAR_VIDEOLLAMADA.bat
```

---

## 🎯 Recomendación

**Usa CMD** (no Git Bash) para ejecutar los scripts `.bat`. Es más simple y funciona mejor.

Para abrir CMD:
1. Presiona `Win + R`
2. Escribe: `cmd`
3. Presiona Enter
4. Navega a tu proyecto: `cd C:\Users\nicoa\Desktop\ZenMindConnect`
5. Ejecuta: `INICIAR_VIDEOLLAMADA.bat`

